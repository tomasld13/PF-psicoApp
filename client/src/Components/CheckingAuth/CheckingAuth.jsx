import Loading from "../Loading/Loading.jsx"

export default function CheckingAuth() {
    return (
        
        <div className="mt-200 bg-white w-screen h-screen absolute flex items-center" >
            <Loading/>
        </div>
    );
}
